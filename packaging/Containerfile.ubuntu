# Ubuntu Noble (24.04)
FROM ubuntu:noble

ENV DEBIAN_FRONTEND=noninteractive

# Added libxkbcommon-dev and liblayershellqtinterface-dev
RUN apt-get update && apt-get install -y \
    build-essential \
    cmake \
    git \
    file \
    dpkg-dev \
    qt6-base-dev \
    qt6-multimedia-dev \
    liblayershellqtinterface-dev \
    libxkbcommon-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /project
